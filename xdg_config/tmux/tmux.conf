# Rebind prefix
unbind C-b
set -g prefix C-Space

# Set default term to tmux to get italics
set -g default-terminal 'tmux-256color'
# Needed for truecolor to work on my devbox
set-option -sa terminal-overrides ",xterm-256color:Tc"

# Use vim bindings
setw -g mode-keys vi

# Enable using the mouse
set -g mouse on

# Escape faster
set -sg escape-time 0

setw -g monitor-activity on
set -g renumber-windows on
set -g detach-on-destroy off
set -g allow-rename on

# switch panes using arrow keys
bind h select-pane -L
bind l select-pane -R
bind k select-pane -U
bind j select-pane -D

# Resize with capitals
bind H resize-pane -L 5
bind L resize-pane -R 5
bind K resize-pane -U 5
bind J resize-pane -D 5

# copy-mode keys
bind v copy-mode
bind -T copy-mode-vi y send-keys -X copy-selection-and-cancel
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi O send-keys -X rectangle-toggle

bind N next-window -a
bind P previous-window -a
bind - last-window

bind S split-window -v
bind s split-window -h

bind C-s command-prompt -p 'save history to filename:' -I '#{pane_current_path}/pane.out' 'capture-pane -S - ; save-buffer %1 ; delete-buffer'

bind C-j join-pane

bind r source-file ~/.tmux.conf

# Set status bar format
set -g status-right "%a %m-%d %R:%S #[fg=#50fa7b]Ξ"
set -g status-left "Ξ "
set -g status-left-length 20
setw -g clock-mode-style 24

set -g history-limit 10240

#### Truecolor Dracula theme
# white='#f8f8f2'
# gray='#44475a'
# dark_gray='#282a36'
# light_purple='#bd93f9'
# dark_purple='#6272a4'
# cyan='#8be9fd'
# green='#50fa7b'
# orange='#ffb86c'
# red='#ff5555'
# pink='#ff79c6'
# yellow='#f1fa8c'

set-option -g status-style fg='#f8f8f2',bg='#44475a'
set-option -g status-left-style fg='#50fa7b'
set-option -g status-right-style fg='#f1fa8c'

set-window-option -g window-status-style fg='#f8f8f2',bg='#44475a'
set-window-option -g window-status-current-style fg='#ff79c6',bg='#44475a'
set-window-option -g window-status-activity-style fg='#fe8019',bg='#44475a'
set-window-option -g window-status-bell-style fg='#44475a',bg='#ff5555'

set-option -g pane-border-style fg='#44475a'
set-option -g pane-active-border-style fg='#6272a4'

set-option -g message-style fg='#f8f8f2',bg='#44475a'
set-option -g message-command-style fg='#ffb86c',bg='#44475a'

set-option -g display-panes-active-colour '#458788'
set-option -g display-panes-colour '#a89984'

set-window-option -g clock-mode-colour '#6272a4'
